import{NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";import{getSupabasePublicConfig}from"@/lib/supabaseConfig";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const config=getSupabasePublicConfig();const db=createClient(config.supabaseUrl,config.supabaseAnonKey);const{data,error}=await db.rpc('get_external_contributor_public',{contributor_uuid:id});if(error||!data?.[0])return NextResponse.json({ok:false},{status:404});return NextResponse.json({ok:true,contributor:data[0]})}
